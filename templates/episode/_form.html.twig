<div class="row d-flex flex-column p-md-1 m-md-1">
	<div class="col-xl-6 col-lg-8 col-md-12 p-2">
		{{ form_start(form) }}
		<div class="row mb-md-3">
			<div class="col py-2">
				{{ form_errors(form) }}
			</div>
		</div>
		<div class="row mb-md-3 py-2">
			<div class="col py-2">
				{{ form_widget(form) }}
			</div>
		</div>
		<div class="row mb-md-3 py-2">
			<div class="col-12">
				<button class="btn btn-sm text-nowrap btn-primary m-2 px-2">{{ button_label|default('Enregistrer') }}</button>
			</div>
		</div>
		{{ form_end(form) }}
	</div>
</div>