{% extends 'base.html.twig' %}

{% block title %}Modifier-Série
{% endblock %}

{% block body %}
	<div class="container-fluid p-md-4">

		<div class="row d-flex mb-3">
			<h1 class="mb-3 p-2">Modifier La Sairie</h1>
			{{ include('program/_form.html.twig', {'button_label': 'Modifier'}) }}
		</div>
		<div class="row d-flex">
			<div class="col d-flex align-items-center justify-content-between me-4">
				{{ include('program/_delete_form.html.twig') }}
				<a class="text-dark fw-bold px-4 fs-3" href="{{ path('app_program_index') }}">Retour</a>
			</div>
		</div>
	</div>
{% endblock %}